# Version floors verified compatible during development (numpy 2.4,
# pandas 3.0, scipy 1.17, scikit-learn 1.8, matplotlib 3.10). torch and
# yfinance floors are set from known-stable API surfaces this project
# uses (auto_adjust=/progress= kwargs on yfinance.download; no torch API
# newer than 2.0 is required) but were not independently re-verified in
# every combination -- if you hit an install conflict, generate a lockfile
# from your own working environment instead of fighting these ranges:
#
#   pip install -r requirements.txt
#   pip freeze > requirements-lock.txt
#
# and commit the lockfile alongside this file for anyone who needs your
# exact reproduction environment.
numpy>=1.26
pandas>=2.1
scipy>=1.12
scikit-learn>=1.3
torch>=2.0
matplotlib>=3.8
yfinance>=0.2.28
pytest>=7.0
